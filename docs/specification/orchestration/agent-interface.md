---
title: Agent 接口
section: orchestration
owner: protocol-architecture
status: skeleton
version: 2026-07-28
# 存量页面迁移：正文为 HTML 原样嵌入；后续编写新内容时可改为纯 Markdown（去掉 format 行）
format: html
---

<h1>Agent 友好接口</h1><p class="draft-note">当前 Draft 尚未规定 Agent 接口要求，不得作为实现依据。</p><h2 id="responses">自描述响应</h2><p>将定义状态、约束和下一步动作。</p><h2 id="errors">结构化错误</h2><p>将定义 Agent 可执行的恢复信息。</p><h2 id="tools">工具边界</h2><p>将定义原语操作与工具声明的对应关系。</p><h2 id="conformance">一致性</h2><p>将定义 Agent 接口的可验证要求。</p>

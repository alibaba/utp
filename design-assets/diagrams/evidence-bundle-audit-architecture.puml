@startuml
title UTP Evidence Bundle 审计架构

skinparam backgroundColor #FFFFFF
skinparam shadowing false
skinparam defaultFontName Arial
skinparam ArrowColor #475569
skinparam rectangleBorderColor #64748B

rectangle "事实产生方\n买方 / 卖方 / 支付服务方 / 履约方" as facts #FEF3C7
rectangle "交易参与方\n记录本方交易事实、授权、\nMandate 与状态结果" as engine #DBEAFE
rectangle "Evidence Bundle\n组件 + 哈希 + Issuer 签名\n可本地保存或委托归档" as bundle #DCFCE7
rectangle "证据消费者\n交易参与方 / 审计方 / 风控系统\n争议解决方 / 监管机构" as consumer #F3E8FF

facts --> engine : 提供事实或凭证
engine --> bundle : 采集并封装
bundle --> consumer : 授权访问 / 导出

note right of bundle
每个 Bundle 只有一个 Issuer，
但 Issuer 不等于所有事实的产生方。
UTP 不要求中心化审计服务。
end note
@enduml
